# commands for ansible dynamic inventory & tags

# aws configure
# ansible-playbook db.yml --ask-vault-pass
# ansible -i aws_ec2.yml -e ansible_user=ec2-user -e ansible_password=DevOps321 dynamic.yml
